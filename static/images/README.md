Place your site logo image here as `finchatbot.jpg`.

- Recommended size: 64x64 or 128x128 pixels (square).
- Supported formats: `.jpg`, `.png`, `.webp`.

After placing the file, reload the site. The templates reference the file at `/static/images/finchatbot.jpg`.
